# Core dependencies
torch>=2.0.0
torchvision
torchaudio

# Qwen3-TTS
qwen-tts>=0.1.0

# Audio processing
soundfile>=0.12.0
pydub>=0.25.0

# Audio transcription & validation (Qwen3-ASR)
qwen-asr>=0.1.0

# Progress & UI
tqdm>=4.65.0
textual>=0.47.0
rich>=13.0.0

# Note: Install PyTorch with CUDA support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
